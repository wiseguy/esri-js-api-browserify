# esri-js-api-browserify
Using Esri JS API 4.0 with browserify and tons of goodness

- Install browserify, npm install -g browserify
- Install watchify, npm install -g watchify
- Run command npm run start

